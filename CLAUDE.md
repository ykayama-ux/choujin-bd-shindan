# Git Workflow

- mainブランチに直接プッシュしない（開発メンバーが複数いるため）
- 作業はフィーチャーブランチで行い、PRを作成してマージする
- コンフリクト防止のため、小さな単位でPRを出す

# セキュリティ（パブリックリポジトリ）

- このリポジトリはPublicのため、コミットした内容はすべて公開される
- APIキー、パスワード、トークンなどの機密情報を絶対にコミットしない
- .env ファイルや認証情報を含むファイルは .gitignore に追加すること
- 個人情報（メールアドレス、電話番号など）をコードにハードコードしない
